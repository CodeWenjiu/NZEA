// use option_parser::OptionParser;
use state::States;

use super::Top;

pub struct Nzea {
    pub top: Top,
    pub states: States,
}

impl Nzea {
    // fn new(option: &OptionParser, states: States, callback: SimulatorCallback) -> Self {

    // }
}
