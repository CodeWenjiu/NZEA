# History Notes

This directory stores historical project notes and migration artifacts.

- `vibe/` is a direct archive of the former `.vibe/` directory.
- Content here is preserved for context and debugging history.
- Archived documents may contain outdated commands or assumptions.

For active contributor workflow, use `AGENTS.md` and `docs/contributor/`.
